/** PostCSS config kept for completeness (ESM — package.json has "type": "module").
 *  The current setup uses the Tailwind Play CDN, so no PostCSS plugins are needed
 *  and this config is effectively a no-op. It becomes active only if you switch
 *  to the official Tailwind PostCSS pipeline. */
export default {
  plugins: {},
};
